# app/

The CFD dashboard web app. Empty as of 2026-09-02. When it exists it follows
`03 App/app/`: static files, ES modules, no bundler, Firebase from the CDN,
`tokens.css` and `components.css` copied in from `06 Design System/`.
