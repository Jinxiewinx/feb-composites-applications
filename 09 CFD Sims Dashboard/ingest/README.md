# ingest/

The uploader that runs on the workstation next to Fluent. Empty as of
2026-09-02. Its first job is to read one real Fluent design-point export
(report PDF plus CSV monitors) and produce the sim record the app will store.
Build it against real data before building any screen.
