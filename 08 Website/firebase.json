{
  "hosting": {
    "site": "feb-team-website",
    "public": "site",
    "ignore": ["firebase.json", "**/.*", "**/node_modules/**", "**/README.md"],
    "headers": [
      {
        "//": "Same policy as the app: always revalidate markup and code so a deploy shows up immediately rather than hiding behind a stale cache.",
        "source": "**/*.@(html|js|css)",
        "headers": [{ "key": "Cache-Control", "value": "no-cache, max-age=0" }]
      },
      {
        "//": "Fonts and images are content-addressed by name and effectively immutable.",
        "source": "**/*.@(woff2|png|jpg|svg|ico)",
        "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
      }
    ]
  }
}
